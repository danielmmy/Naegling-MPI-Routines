## Makefile.am -- Process this file with automake to produce Makefile.in
bin_PROGRAMS = naegling

AM_LDFLAGS =  -lpthread $(BOOST_LIB_SUFFIX)

naegling_SOURCES = naegling-main.c naegling-main.h mpi-control.c mpi-control.h naegling-com.c naegling-com.h 
